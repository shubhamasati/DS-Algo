#include <iostream>
#include <vector>
#include <cmath>

using namespace std;

int solve(vector<int> &A){
    int ans;
    int N = A.size();
    
    
    for(int i = 1; i < N; i++){
        
    }
}

int main(){

    int N;
    cin >> N;

    vector<int> A(N);
    for(int i = 0; i <N; i++) cin >> A[i];

    int ans = solve(A);
    cout << ans << endl;
}